.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# local env files
.dev.vars

# Claude Code local settings (per-user overrides)
.claude/settings.local.json

# Cloudflare Wrangler local state
.wrangler/
.gstack/

# External docs cache (fetched on demand via `pnpm docs:fetch`)
docs/.cache/
